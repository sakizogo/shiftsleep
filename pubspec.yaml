name: shiftsleep
description: "交代勤務者向け睡眠管理アプリ。分割睡眠・シフト対応・医学的信頼性を重視。"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.1.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  provider: ^6.1.5+1
  sqflite: ^2.3.0
  path: ^1.8.3
  table_calendar: ^3.0.9
  flutter_local_notifications: ^17.1.0   # ← 追加
  timezone: ^0.9.2                       # ← 追加
  uuid: ^4.0.0                          # ← 追加

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true